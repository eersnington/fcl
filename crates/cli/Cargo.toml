[package]
name = "fcl"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.47", features = ["derive"] }
fcl-core = { path = "../core" }

[dev-dependencies]
tempfile = "3.22.0"

[lints]
workspace = true
